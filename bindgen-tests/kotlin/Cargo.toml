[package]
name = "uniffi-bindgen-tests-kotlin"
version = "0.31.0"
edition = "2021"

[dev-dependencies]
camino = "1"
glob = "0.3"
uniffi = { path = "../../uniffi", features = ["bindgen"] }
uniffi-bindgen-tests = { path = "../lib" }
