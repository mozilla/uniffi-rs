namespace documentation {
  string hello(Pet pet);
  u64 add(u64 a, u64 b);
};

dictionary Pet {
  string name;
};

interface Person {
  constructor(string name);
  void set_name(string name);
  string get_name();
};

enum TestEnum {
  "A",
  "B",
  "C",
};
