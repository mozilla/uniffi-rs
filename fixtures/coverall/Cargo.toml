[package]
name = "uniffi-fixture-coverall"
version = "0.22.0"
edition = "2021"
license = "MPL-2.0"
publish = false

[lib]
crate-type = ["lib", "cdylib"]
name = "uniffi_coverall"

[dependencies]
# Add the "pointer-ffi" feature to make sure things can build correctly
uniffi = { workspace = true, features=["pointer-ffi"]}
once_cell = "1.12"
thiserror = "2"

[build-dependencies]
uniffi = { workspace = true, features = ["build", "pointer-ffi"] }

[dev-dependencies]
uniffi = { workspace = true, features = ["bindgen-tests"] }
uniffi_meta = { path = "../../uniffi_meta/" }

[features]
ffi-trace = ["uniffi/ffi-trace"]
