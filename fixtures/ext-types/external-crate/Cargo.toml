[package]
name = "uniffi-fixture-ext-types-external-crate"
edition = "2021"
version = "0.22.0"
authors = ["Firefox Sync Team <sync-team@mozilla.com>"]
license = "MPL-2.0"
publish = false

[lib]
crate-type = ["lib", "cdylib"]
name = "ext_types_external_crate"

[dependencies]
# NOTE: This crate should never depend on UniFFI - see README.md for more.

[build-dependencies]

[dev-dependencies]
