[package]
name = "uniffi-fixture-futures"
version = "0.21.0"
edition = "2021"
license = "MPL-2.0"
publish = false

[lib]
name = "uniffi_futures"
crate-type = ["lib", "cdylib"]

[[bin]]
name = "uniffi-fixtures-futures"
path = "src/bin.rs"

[dependencies]
# Add the "pointer-ffi" feature to make sure things can build correctly
uniffi = { workspace = true, features = ["tokio", "cli", "pointer-ffi"] }
async-trait = "0.1"
futures = "0.3"
thiserror = "2"
tokio = { version = "1.38.2", features = ["time", "sync"] }
once_cell = "1.18.0"

[build-dependencies]
uniffi = { workspace = true, features = ["build", "pointer-ffi"] }

[dev-dependencies]
uniffi = { workspace = true, features = ["bindgen-tests"] }

[features]
ffi-trace = ["uniffi/ffi-trace"]
