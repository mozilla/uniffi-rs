[package]
name = "swift-perform-checksum-once"
version = "0.27.1"
authors = ["Matías Bzurovski <matibzurovski@gmail.com>", "Alexander Cyon <alex.cyon@gmail.com>"]
edition = "2021"
license = "MPL-2.0"
publish = false

[lib]
crate-type = ["lib", "cdylib"]
name = "swift_perform_checksum_once"

[dependencies]
uniffi = { workspace = true }

[build-dependencies]
uniffi = { workspace = true, features = ["build"] }

[dev-dependencies]
uniffi = { workspace = true, features = ["bindgen-tests"] }
