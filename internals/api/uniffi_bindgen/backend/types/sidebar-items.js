window.SIDEBAR_ITEMS = {"trait":[["CodeType","A Trait to emit foreign language code to handle referenced types. A type which is specified in the UDL (i.e. a member of the component interface) will have a `CodeDeclaration` as well, but for types used e.g. primitive types, Strings, etc only a `CodeType` is needed."],["CodeTypeDispatch","This trait is used to implement `CodeType` for `Type` and type-like structs (`Record`, `Enum`, `Field`, etc).  We forward all method calls to a `Box<dyn CodeType>`, which we get by calling `CodeOracle.find()`."]]};